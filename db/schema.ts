import { sqliteTable, text, integer, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const users=sqliteTable('users',{id:text().primaryKey(),email:text().notNull().unique(),name:text().notNull(),password:text(),verified:integer().notNull().default(0),role:text().notNull().default('worker'),bio:text().notNull().default(''),city:text().notNull().default(''),skills:text().notNull().default(''),experience:integer().notNull().default(0),expectedPay:integer('expected_pay').notNull().default(0),availableFrom:text('available_from').notNull().default(''),availableTo:text('available_to').notNull().default(''),company:text().notNull().default(''),suspended:integer().notNull().default(0),createdAt:text('created_at').notNull()});
export const identities=sqliteTable('identities',{provider:text().notNull(),subject:text().notNull(),userId:text('user_id').notNull().references(()=>users.id)},t=>[uniqueIndex('idx_identity_provider_subject').on(t.provider,t.subject)]);
export const sessions=sqliteTable('sessions',{hash:text().primaryKey(),userId:text('user_id').notNull().references(()=>users.id),expires:integer().notNull()});
export const challenges=sqliteTable('challenges',{hash:text().primaryKey(),kind:text().notNull(),payload:text().notNull(),expires:integer().notNull()});
export const limits=sqliteTable('rate_limits',{key:text().primaryKey(),count:integer().notNull(),expires:integer().notNull()});
export const jobs=sqliteTable('jobs',{id:text().primaryKey(),ownerId:text('owner_id').notNull().references(()=>users.id),title:text().notNull(),description:text().notNull(),category:text().notNull(),city:text().notNull(),skills:text().notNull(),start:text().notNull(),duration:integer().notNull(),budget:integer().notNull(),experience:integer().notNull().default(0),status:text().notNull().default('open'),workerId:text('worker_id').references(()=>users.id),createdAt:text('created_at').notNull()},t=>[index('idx_jobs_status_start').on(t.status,t.start),index('idx_jobs_owner').on(t.ownerId)]);
export const applications=sqliteTable('applications',{id:text().primaryKey(),jobId:text('job_id').notNull().references(()=>jobs.id),workerId:text('worker_id').notNull().references(()=>users.id),note:text().notNull(),status:text().notNull().default('applied'),createdAt:text('created_at').notNull()},t=>[uniqueIndex('idx_application_job_worker').on(t.jobId,t.workerId),index('idx_applications_worker').on(t.workerId)]);
export const saved=sqliteTable('saved',{userId:text('user_id').notNull().references(()=>users.id),jobId:text('job_id').notNull().references(()=>jobs.id)},t=>[uniqueIndex('idx_saved_user_job').on(t.userId,t.jobId)]);
export const payments=sqliteTable('payments',{jobId:text('job_id').primaryKey().references(()=>jobs.id),amount:integer().notNull(),commission:integer().notNull(),status:text().notNull().default('pending'),reference:text().notNull().default(''),createdAt:text('created_at').notNull(),paidAt:text('paid_at')});
export const reviews=sqliteTable('reviews',{id:text().primaryKey(),jobId:text('job_id').notNull().references(()=>jobs.id),authorId:text('author_id').notNull().references(()=>users.id),targetId:text('target_id').notNull().references(()=>users.id),rating:integer().notNull(),comment:text().notNull(),createdAt:text('created_at').notNull()},t=>[uniqueIndex('idx_review_job_author').on(t.jobId,t.authorId),index('idx_reviews_target').on(t.targetId)]);
export const audit=sqliteTable('audit',{id:text().primaryKey(),actorId:text('actor_id').notNull(),action:text().notNull(),targetId:text('target_id').notNull(),createdAt:text('created_at').notNull()});
